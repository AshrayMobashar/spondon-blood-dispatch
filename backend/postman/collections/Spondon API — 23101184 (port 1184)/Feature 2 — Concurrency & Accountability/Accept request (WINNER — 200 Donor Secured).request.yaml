$kind: http-request
url: "{{baseUrl}}/api/requests/{{request2_id}}/accept"
method: POST
headers:
  Content-Type: application/json
body:
  type: text
  content: |-
    {
      "donor_id": "{{donor_id}}"
    }
order: 2000
